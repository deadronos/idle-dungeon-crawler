# Regional Progression Architecture Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.\n\n**Goal:** Implement region-aware progression with data-driven region definitions, per-region state tracking, and region-aware encounter generation, aligned with Decision 015.\n\n**Architecture:** Add a `RegionDefinition` data model and `RegionProgress` tracking to the game state. Replace raw global floor logic with a `FloorContext` (region + local floor) in encounter generation. Split region-specific actions into a dedicated zustand slice. Update persistence, UI, and tests accordingly.\n\n**Tech Stack:** React 19, TypeScript, Zustand, Vite, Vitest\n\n---\n\n## File Map\n\n| File | Responsibility | Action |\n|------|---------------|--------|\n| `src/game/regions.ts` | Region definitions (`RegionDefinition`, `RegionProgress`), lookup helpers, default region data | **Create** |\n| `src/game/regions.test.ts` | Unit tests for region helpers, floor context, default state | **Create** |\n| `src/game/store/types.ts` | Add `currentRegionId`, `currentRegionFloor`, `regionProgress`, `highestRegionFloorCleared` to `GameState` and `ProgressionSlice` | **Modify** |\n| `src/game/store/regionSlice.ts` | Zustand slice: `selectRegionState`, `createRegionSlice` with actions (`advanceInRegion`, `changeRegion`, etc.) | **Create** |\n| `src/game/store/regionSlice.test.ts` | Unit tests for the region slice actions and state transitions | **Create** |\n| `src/game/store/gameStore.tsx` | Wire `createRegionSlice` into the main store alongside existing slices | **Modify** |\n| `src/game/store/selectors.ts` | Add `selectCurrentRegion` and `selectRegionName` selectors for UI consumption | **Modify** |\n| `src/game/engine/encounter.ts` | Refactor encounter generation to use `FloorContext` instead of raw `floor` number; update `isBossFloor`, `getEncounterSize`, `createEncounter` to use region data | **Modify** |\n| `src/game/engine/encounter.test.ts` | (or create new) Tests for region-aware encounter generation | **Create/Modify** |\n| `src/components/MainGameView.tsx` | Display current region name alongside floor number | **Modify** |\n| `src/game/store/persistence.migrations.ts` | Add `SAVE_MIGRATIONS[5]` to initialize new region fields on old saves | **Modify** |\n| `src/game/store/persistence.validation.ts` | Add `sanitizeRegionProgress` to safely parse region progress from saves | **Modify** |\n| `src/game/store/persistence.test.ts` | (or create new) Tests for save migration and deserialization with new fields | **Create/Modify** |\n| `docs/ARCHITECTURE/technicaldecisions/016-regional-progression-implementation.md` | Document the implementation details and any deviations from Decision 015 | **Create** |\n\n---\n\n## Task 1: Define Region Data Model\n\n**Files:**\n- Create: `src/game/regions.ts`\n- Test: `src/game/regions.test.ts`\n\n- [ ] **Step 1: Write the failing test**\n\n```typescript\n// src/game/regions.test.ts\nimport { describe, expect, it } from \"vitest\";\nimport { getDefaultRegionProgress, getRegionDefinition, getStartingFloorContext } from \"./regions\";\n\ndescribe(\"region data model\", () => {\n  it(\"should provide a default region definition for Dank Cellar\", () => {\n    const def = getRegionDefinition(\"dank cellar\");\n    expect(def.name).toBe(\"Dank Cellar\");\n    expect(def.localFloorEnd).toBe(50);\n    expect(def.nextRegionId).toBe(\"forgotten tunnels\");\n  });\n\n  it(\"should return the starting floor context\", () => {\n    const ctx = getStartingFloorContext();\n    expect(ctx.regionId).toBe(\"dank cellar\");\n    expect(ctx.localFloor).toBe(1);\n    expect(ctx.globalFloor).toBe(1);\n  });\n\n  it(\"should provide empty region progress\", () => {\n    const progress = getDefaultRegionProgress();\n    expect(progress[\"dank cellar\"]).toBeDefined();\n    expect(progress[\"dank cellar\"].highestLocalFloorCleared).toBe(0);\n    expect(progress[\"dank cellar\"].unlocked).toBe(true);\n  });\n});\n```\n\n- [ ] **Step 2: Run test to verify it fails**\n\nRun: `npx vitest run src/game/regions.test.ts`\nExpected: FAIL with `Cannot find module './regions'`\n\n- [ ] **Step 3: Write minimal implementation**\n\nCreate `src/game/regions.ts` with:\n- `RegionDefinition` interface (id, name, order, localFloorStart, localFloorEnd, backgroundAsset, enemyRoster, milestoneDefinitions, completionFloor, nextRegionId, statBandMultiplier)\n- `RegionProgress` interface (highestLocalFloorCleared, unlocked, completed)\n- `FloorContext` interface (regionId, localFloor, globalFloor – initially globalFloor can be derived)\n- `REGION_DEFINITIONS` constant with Dank Cellar and a scaffolded Forgotten Tunnels\n- `getRegionDefinition(regionId)` lookup\n- `getDefaultRegionProgress()` constructing initial progress for unlocked default region\n- `getStartingFloorContext()` returning `{ regionId: 'dank cellar', localFloor: 1 }`\n\n- [ ] **Step 4: Run test to verify it passes**\n\nRun: `npx vitest run src/game/regions.test.ts`\nExpected: PASS\n\n- [ ] **Step 5: Commit**\n\n```bash\ngit add src/game/regions.ts src/game/regions.test.ts\ngit commit -m \"feat(regions): define region data model and default progress\"\n```\n\n---\n\n## Task 2: Extend GameState with Region Fields\n\n**Files:**\n- Modify: `src/game/store/types.ts`\n- Modify: `src/game/engine/encounter.ts` (to accept FloorContext in select functions, minimal refactor)\n\n- [ ] **Step 1: Write the failing test**\n\nIn `src/game/regions.test.ts`, add a test that checks a `FloorContext` can be passed to `getEncounterSize` and `isBossFloor`:\n\n```typescript\n  it(\"should use FloorContext for encounter generation\", () => {\n    const ctx: FloorContext = { regionId: \"dank cellar\", localFloor: 10 };\n    expect(isBossFloor(ctx)).toBe(true);\n    expect(getEncounterSize(ctx)).toBe(1);\n  });\n```\n\n- [ ] **Step 2: Run test to verify it fails**\n\nRun: `npx vitest run src/game/regions.test.ts`\nExpected: FAIL – `isBossFloor` and `getEncounterSize` don't exist in `regions.ts` yet (they are in `encounter.ts`)\n\n- [ ] **Step 3: Write minimal implementation**\n\n1. In `src/game/store/types.ts`, add to `ProgressionSlice`:\n   - `currentRegionId: string`\n   - `currentRegionFloor: number`\n   - `regionProgress: Record<string, RegionProgress>` (imported from `regions.ts`)\n   - `highestRegionFloorCleared: number` (optional – derived from `regionProgress`)\n2. In `src/game/regions.ts`, export `isBossFloor(context: FloorContext)` and `getEncounterSize(context: FloorContext)` that delegate to the existing logic but use `context.localFloor`.\n3. Keep backward compatibility: accept `number | FloorContext` in these functions and normalize internally.\n\n- [ ] **Step 4: Run test to verify it passes**\n\nRun: `npx vitest run src/game/regions.test.ts`\nExpected: PASS\n\n- [ ] **Step 5: Commit**\n\n```bash\ngit add src/game/store/types.ts src/game/regions.ts src/game/regions.test.ts\ngit commit -m \"feat(types): extend GameState and encounter logic with FloorContext\"\n```\n\n---\n\n## Task 3: Create Region Zustand Slice\n\n**Files:**\n- Create: `src/game/store/regionSlice.ts`\n- Test: `src/game/store/regionSlice.test.ts`\n\n- [ ] **Step 1: Write the failing test**\n\n```typescript\n// src/game/store/regionSlice.test.ts\nimport { describe, expect, it } from \"vitest\";\nimport { createRegionSlice } from \"./regionSlice\";\nimport type { GameState } from \"./types\";\n\nconst mockSet = (fn: (state: GameState) => Partial<GameState>) => fn({} as GameState);\nconst mockGet = () => ({} as GameState);\n\ndescribe(\"region slice\", () => {\n  it(\"should create a slice with default region state\", () => {\n    const slice = createRegionSlice({\n      currentRegionId: \"dank cellar\",\n      currentRegionFloor: 1,\n      regionProgress: { \"dank cellar\": { highestLocalFloorCleared: 0, unlocked: true, completed: false } },\n    })(mockSet, mockGet);\n    expect(slice.currentRegionId).toBe(\"dank cellar\");\n    expect(slice.currentRegionFloor).toBe(1);\n  });\n});\n```\n\n- [ ] **Step 2: Run test to verify it fails**\n\nRun: `npx vitest run src/game/store/regionSlice.test.ts`\nExpected: FAIL – `createRegionSlice` not found\n\n- [ ] **Step 3: Write minimal implementation**\n\nCreate `src/game/store/regionSlice.ts`:\n- Define `RegionSlice` extending `RegionState` + `RegionActions`.\n- Export `selectRegionState`.\n- Export `createRegionSlice` with actions:\n  - `advanceToNextFloor(): void` – increments `currentRegionFloor`, updates `regionProgress` for that region if higher, checks completion.\n  - `changeRegion(regionId: string): void` – switches active region (only if unlocked).\n  - `unlockNextRegion(): void` – marks next region as unlocked if current is completed.\n  - `getIsRegionCompleted(): boolean` – derived from progress.\n\n- [ ] **Step 4: Run test to verify it passes**\n\nRun: `npx vitest run src/game/store/regionSlice.test.ts`\nExpected: PASS\n\n- [ ] **Step 5: Commit**\n\n```bash\ngit add src/game/store/regionSlice.ts src/game/store/regionSlice.test.ts\ngit commit -m \"feat(store): add region zustand slice with core actions\"\n```\n\n---\n\n## Task 4: Wire Region Slice into GameStore\n\n**Files:**\n- Modify: `src/game/store/gameStore.tsx`\n- Modify: `src/game/store/types.ts` (add `RegionActions` into `GameActions`)\n\n- [ ] **Step 1: Write the failing test**\n\nIn `src/game/store/gameStore.test.ts`, assert that the store exposes region state and that a reset preserves default region progress:\n\n```typescript\n  it(\"should expose region state from the store\", () => {\n    const store = createGameStore();\n    expect(store.getState().currentRegionId).toBe(\"dank cellar\");\n    expect(store.getState().currentRegionFloor).toBe(1);\n  });\n```\n\n- [ ] **Step 2: Run test to verify it fails**\n\nRun: `npx vitest run src/game/store/gameStore.test.ts`\nExpected: FAIL – `currentRegionId` not on store\n\n- [ ] **Step 3: Write minimal implementation**\n\n1. In `src/game/store/types.ts`, add `RegionActions` to `GameActions`.\n2. In `src/game/store/gameStore.tsx`, import `createRegionSlice` and `selectRegionState`, and merge them into the `createStore` call.\n3. Update `createInitialGameState` in `encounter.ts` to set `currentRegionId`, `currentRegionFloor`, and `regionProgress` defaults.\n\n- [ ] **Step 4: Run test to verify it passes**\n\nRun: `npx vitest run src/game/store/gameStore.test.ts`\nExpected: PASS\n\n- [ ] **Step 5: Commit**\n\n```bash\ngit add src/game/store/gameStore.tsx src/game/store/types.ts src/game/engine/encounter.ts src/game/store/gameStore.test.ts\ngit commit -m \"feat(store): wire region slice into main game store\"\n```\n\n---\n\n## Task 5: Make Encounter Generation Region-Aware\n\n**Files:**\n- Refactor: `src/game/engine/encounter.ts`\n\n- [ ] **Step 1: Write the failing test**\n\nIn `src/game/engine/encounter.test.ts` (or create if missing):\n\n```typescript\n  it(\"creates region-aware encounters using FloorContext\", () => {\n    const ctx = { regionId: \"dank cellar\", localFloor: 5 };\n    const enemies = createEncounter(ctx);\n    expect(enemies.length).toBeGreaterThan(0);\n    expect(enemies[0].level).toBe(5);\n  });\n```\n\n- [ ] **Step 2: Run test to verify it fails**\n\nRun: `npx vitest run src/game/engine/encounter.test.ts`\nExpected: FAIL – `createEncounter` still expects a raw `floor: number`\n\n- [ ] **Step 3: Write minimal implementation**\n\n1. Update `createEncounter(floor: number)` to `createEncounter(context: FloorContext | number)` – normalize to local floor.\n2. Update `getEncounterSize`, `isBossFloor`, `getEnemyArchetypePoolForFloor`, `getEncounterArchetypes`, `getEnemyElementForEncounter` to accept `FloorContext | number`.\n3. Ensure the stat band scaling uses the region's `statBandMultiplier`.\n\n- [ ] **Step 4: Run test to verify it passes**\n\nRun: `npx vitest run src/game/engine/encounter.test.ts`\nExpected: PASS\n\n- [ ] **Step 5: Commit**\n\n```bash\ngit add src/game/engine/encounter.ts src/game/engine/encounter.test.ts\ngit commit -m \"feat(engine): make encounter generation region-aware via FloorContext\"\n```\n\n---\n\n## Task 6: Update Persistence (Migrations & Validation)\n\n**Files:**\n- Modify: `src/game/store/persistence.migrations.ts`\n- Modify: `src/game/store/persistence.validation.ts`\n- Modify: `src/game/store/persistence.types.ts` (if needed)\n\n- [ ] **Step 1: Write the failing test**\n\nIn `src/game/store/persistence.test.ts` (create if missing):\n\n```typescript\n  it(\"migrates legacy saves to include region progress\", () => {\n    const legacySave = { version: 0, state: { floor: 10 } };\n    const migrated = migrateSaveEnvelope(normalizeSaveEnvelope(legacySave));\n    expect(migrated.state.currentRegionId).toBe(\"dank cellar\");\n    expect(migrated.state.regionProgress).toBeDefined();\n  });\n```\n\n- [ ] **Step 2: Run test to verify it fails**\n\nRun: `npx vitest run src/game/store/persistence.test.ts`\nExpected: FAIL – new fields not in migration\n\n- [ ] **Step 3: Write minimal implementation**\n\n1. Add `sanitizeRegionProgress` in `persistence.validation.ts`.\n2. Add `SAVE_MIGRATIONS[5]` in `persistence.migrations.ts` that injects:\n   - `currentRegionId: 'dank cellar'`\n   - `currentRegionFloor: state.floor ?? 1`\n   - `regionProgress: { 'dank cellar': { highestLocalFloorCleared: state.highestFloorCleared ?? 0, unlocked: true, completed: false } }`\n3. Bump `GAME_STATE_EXPORT_VERSION`.\n\n- [ ] **Step 4: Run test to verify it passes**\n\nRun: `npx vitest run src/game/store/persistence.test.ts`\nExpected: PASS\n\n- [ ] **Step 5: Commit**\n\n```bash\ngit add src/game/store/persistence*.ts src/game/store/persistence.test.ts\ngit commit -m \"feat(save): add region progress migration and validation\"\n```\n\n---\n\n## Task 7: Update UI in MainGameView\n\n**Files:**\n- Modify: `src/components/MainGameView.tsx`\n- Modify: `src/game/store/selectors.ts`\n\n- [ ] **Step 1: Write the failing test**\n\nIn `src/components/MainGameView.test.tsx` (create if missing):\n\n```typescript\n  it(\"renders the current region name\", () => {\n    render(<MainGameView />, { wrapper: GameProvider });\n    expect(screen.getByText(/Dank Cellar/i)).toBeInTheDocument();\n  });\n```\n\n- [ ] **Step 2: Run test to verify it fails**\n\nRun: `npx vitest run src/components/MainGameView.test.tsx`\nExpected: FAIL – no region text rendered\n\n- [ ] **Step 3: Write minimal implementation**\n\n1. Add `selectCurrentRegion` and `selectRegionName` to `src/game/store/selectors.ts`.\n2. In `MainGameView.tsx`, use `useGameStore(selectCurrentRegion)` to get the region name.\n3. Render the region name above or next to the floor number, styled consistently (uppercase, tracking, amber/slate color).\n\n- [ ] **Step 4: Run test to verify it passes**\n\nRun: `npx vitest run src/components/MainGameView.test.tsx`\nExpected: PASS\n\n- [ ] **Step 5: Commit**\n\n```bash\ngit add src/components/MainGameView.tsx src/components/MainGameView.test.tsx src/game/store/selectors.ts\ngit commit -m \"feat(ui): display current region name in MainGameView\"\n```\n\n---\n\n## Task 8: Final Verification\n\nRun the full test suite and verify the build compiles without errors.\n\n- [ ] **Step 1: Run all tests**\n\n```bash\nnpx vitest run\n```\n\nExpected: All tests pass. If any existing tests broke due to the `floor → FloorContext` refactor, fix them now by adding backward-compatible overloads.\n\n- [ ] **Step 2: Run build**\n\n```bash\nnpx tsc --noEmit && npx vite build\n```\n\nExpected: No TypeScript or build errors.\n\n- [ ] **Step 3: Write architecture decision doc**\n\nCreate `docs/ARCHITECTURE/technicaldecisions/016-regional-progression-implementation.md` summarizing what was implemented, any deviations from Decision 015, and how saves are migrated.\n\n- [ ] **Step 4: Commit**\n\n```bash\ngit add docs/ARCHITECTURE/technicaldecisions/016-regional-progression-implementation.md\ngit commit -m \"docs: record regional progression implementation decisions\"\n```\n\n---\n\n## Post-Implementation (not in this plan)\n\n- Region selection UI (switch between unlocked regions)\n- Forgotten Tunnels content authoring (enemy roster, bosses, milestones)\n- Ascension layer design (Decision 013 follow-up)\n- Visual theme switching per region background\n", "filePath"]}
